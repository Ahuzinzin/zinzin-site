.vercel
.DS_Store

# Animation master files (huge ProRes originals) — the site uses the .webm exports
# (Chrome/Firefox) and the HEVC-with-alpha .mp4 re-encodes (Safari, no WebM alpha support)
Bakerybonanza/Animation/*.mov
