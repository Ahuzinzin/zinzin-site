.vercel
.DS_Store

# Raw animation / video masters — the repo tracks only the web exports
# (.webm, and the stacked-alpha .mp4 re-encodes used by every browser).
Bakerybonanza/Animation/*.mov
Joker's Madhouse/
*.MOV
banner3.MOV
banner4.MOV

# Scratch / probe files
Untitled-*.jpg
**/_write_test*.txt
