.vercel
.DS_Store

# Animation master files (huge ProRes originals) — the .webm exports are used on the site,
# except the HEVC-with-alpha .mov re-encodes below, which are served to Safari (no WebM alpha support)
Bakerybonanza/Animation/*.mov
!Bakerybonanza/Animation/character_Idle_alpha.mov
!Bakerybonanza/Animation/character_touch_alpha.mov
